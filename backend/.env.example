# Backend — only needed when NOT using docker-compose
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/slotfinder

# API key for protected endpoints (auto-generated if empty)
API_KEY=change-me-to-a-secret-key
